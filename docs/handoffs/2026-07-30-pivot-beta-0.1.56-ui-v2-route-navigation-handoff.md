# Pivot Beta-0.1.56 UI V2 route navigation handoff

Date: 2026-07-30  
Visible version: `Beta-0.1.56`  
Package/updater SemVer: `0.1.56-beta`

## Completed slice

- Removed `AppMode`, `setMode` and `toggleMode` from the active UI Store.
- Replaced the active `IdeActivity` / `WorkbenchTab` state names with narrow `WorkspaceActivity` / `SessionView` contracts.
- Moved `PivotRoute`, `SessionView` and shortcut resolution into `src/renderer/navigation/pivot-navigation.ts`.
- Replaced the two-product first-run choice with one outcome-first entry.
- Kept conversation, editor, preview and terminal reachable as session views.
- Added pure navigation behavior tests, structural boundary tests and a real Electron route/shortcut E2E.

## Invariants preserved

- Session, Work, Artifact, Preview and Terminal surfaces remain available.
- Renderer receives no Main implementation, database, filesystem or Admin capability.
- Guarded Safe Write and all Axis Main-only action foundations are unchanged.
- This slice does not make Dynamic Pivot actions production reachable.

## Verification

- `npm.cmd test`: 164 files / 653 tests passed.
- `npm.cmd exec tsc -- --noEmit`: passed.
- `npm.cmd run build`: passed.
- `npm.cmd run verify:performance`: passed.
- `npm.cmd run rebuild:native:electron`: passed.
- `node scripts/e2e-smoke.mjs --now`: passed.
- `npm.cmd exec playwright test tests/e2e/route-navigation-v2.spec.ts`: 1/1 passed.
- `npm.cmd exec playwright test tests/e2e/settings-v2.spec.ts`: 1/1 passed.

No installer or portable package was generated.

## Next recommended slice

Production-compose the reviewed Dynamic Pivot actions behind one guarded Main-owned consumption boundary, beginning with explicit stop and human-escalation semantics. Keep scheduling, Worker execution and authority issuance separate and test real failure/restart paths before exposing anything to Renderer.
