# Pivot Beta-1.20.0 semantic Reviewer policy handoff

- Added a narrow Main-only `AxisSemanticReviewerPort` returning untrusted `unknown`.
- Added code-owned fail-closed policy for malformed, stale and low-confidence output.
- Security rejection routes to dedicated Fixer; correctness rejection routes to retry.
- No Renderer, Worker, database or filesystem capability was introduced.
